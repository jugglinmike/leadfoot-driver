{
  "name": "leadfoot-driver",
  "version": "0.2.0",
  "description": "An API for defining abstract user interactions for UI tests using the Leadfoot bindings for Selenium",
  "main": "src/index.js",
  "scripts": {
    "lint": "jshint src test",
    "test-unit": "mocha --ui tdd --recursive test/setup.js test/tests",
    "test": "npm run lint && npm run test-unit"
  },
  "author": "Mike Pennisi",
  "license": "MIT",
  "peerDependencies": {
    "leadfoot": "^1.6.4"
  },
  "devDependencies": {
    "bluebird": "^2.10.2",
    "jshint": "^2.8.0",
    "mocha": "^2.3.3"
  }
}
